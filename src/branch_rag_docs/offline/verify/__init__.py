"""Chặng 6 — đối soát.

    inspect_chunks.py  thống kê phân bố chunk

Chạy tay, `pipeline.py` không gọi.

CHƯA CÓ:

    coverage.py  số trang trong pdf = số block = số chunk = số vector trong store
    trace.py     lấy ngẫu nhiên vector -> đi ngược provenance về trang trong pdf
"""

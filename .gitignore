__pycache__/
*.py[cod]
.venv/
venv/
.env
*.egg-info/
dist/
build/

.pytest_cache/
.ruff_cache/
.mypy_cache/

# Artifact của pipeline được commit vào repo: clone về là chạy src.online_sql
# ngay, không phải chạy lại src.offline_sql trước.
# Muốn quay lại chặn thì bỏ comment 2 dòng dưới (phủ định chỉ có tác dụng khi
# thư mục cha không bị chặn).
# data/artifacts/*
# !data/artifacts/.gitkeep

# File gốc người dùng upload — không nằm trong repo.
# data/uploads/*
# !data/uploads/.gitkeep

# Tài liệu nguồn: commit vào rồi xoá vẫn còn trong lịch sử.
# *.pdf
# *.docx
# *.doc
# *.pptx
# *.xlsx

# Cache model của HuggingFace nếu ai đó trỏ HF_HOME vào repo
.cache/
models/
